import { RECT_NUMBER_TYPE } from "@visactor/vrender-core";

import { RoughBaseRender } from "./base-render";

export class RoughCanvasRectRender extends RoughBaseRender {
    constructor(canvasRenderer) {
        super(), this.canvasRenderer = canvasRenderer, this.style = "rough", this.type = "rect", 
        this.numberType = RECT_NUMBER_TYPE;
    }
    draw(rect, renderService, drawContext, params) {
        this.doDraw(rect, renderService, drawContext, params);
    }
}
//# sourceMappingURL=rough-rect.js.map
