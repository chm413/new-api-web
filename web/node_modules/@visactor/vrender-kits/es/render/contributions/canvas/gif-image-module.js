import { GraphicRender } from "@visactor/vrender-core/render/symbol";

import { ImageRenderContribution } from "@visactor/vrender-core";

import { createContributionProvider } from "../../../common/explicit-binding";

import { DefaultCanvasGifImageRender } from "./gif-image-render";

let loadGifImageModule = !1;

export function bindGifImageRenderContribution(container) {
    loadGifImageModule || (loadGifImageModule = !0, container.bind(DefaultCanvasGifImageRender).toDynamicValue((() => new DefaultCanvasGifImageRender(createContributionProvider(ImageRenderContribution, container)))).inSingletonScope(), 
    container.bind(GraphicRender).toService(DefaultCanvasGifImageRender));
}
//# sourceMappingURL=gif-image-module.js.map
