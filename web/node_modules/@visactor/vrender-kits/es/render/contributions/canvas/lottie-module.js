import { GraphicRender } from "@visactor/vrender-core/render/symbol";

import { RectRenderContribution } from "@visactor/vrender-core";

import { createContributionProvider } from "../../../common/explicit-binding";

import { DefaultCanvasLottieRender } from "./lottie-render";

let loadLottieModule = !1;

export function bindLottieRenderContribution(container) {
    loadLottieModule || (loadLottieModule = !0, container.bind(DefaultCanvasLottieRender).toDynamicValue((() => new DefaultCanvasLottieRender(createContributionProvider(RectRenderContribution, container)))).inSingletonScope(), 
    container.bind(GraphicRender).toService(DefaultCanvasLottieRender));
}
//# sourceMappingURL=lottie-module.js.map
