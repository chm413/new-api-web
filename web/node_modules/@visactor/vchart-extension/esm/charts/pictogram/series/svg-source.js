import { registerDataSetInstanceParser, warn } from "@visactor/vchart";

import { DataSet, DataView, svgParser } from "@visactor/vchart";

export const svgSourceMap = new Map;

export const SVG_VIEWPORT_RECT_KEY = "_vchartViewportRect";

let svgDataSet;

const numberReg = /-?(?:\d+\.?\d*|\.\d+)(?:e[-+]?\d+)?/gi;

function initSVGDataSet() {
    svgDataSet || (svgDataSet = new DataSet, registerDataSetInstanceParser(svgDataSet, "svg", svgParser));
}

function parseEnableBackgroundViewport(source) {
    var _a, _b, _c, _d;
    const svgStartTag = null === (_a = source.match(/<svg\b[\s\S]*?>/i)) || void 0 === _a ? void 0 : _a[0], enableBackground = null === (_b = null == svgStartTag ? void 0 : svgStartTag.match(/\benable-background\s*=\s*(['"])(.*?)\1/i)) || void 0 === _b ? void 0 : _b[2];
    if (!enableBackground || !/^\s*new\b/i.test(enableBackground)) return null;
    const values = null !== (_d = null === (_c = enableBackground.match(numberReg)) || void 0 === _c ? void 0 : _c.map(Number)) && void 0 !== _d ? _d : [], [x, y, width, height] = values;
    return !Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(width) || !Number.isFinite(height) || width <= 0 || height <= 0 ? null : {
        x: x,
        y: y,
        width: width,
        height: height
    };
}

function hasValidSize(parsedSvg) {
    return Number.isFinite(parsedSvg.width) && Number.isFinite(parsedSvg.height) && parsedSvg.width > 0 && parsedSvg.height > 0;
}

export function registerSVGSource(key, source) {
    svgSourceMap.has(key) && warn(`svg source key of '${key}' already exists, will be overwritten.`), 
    initSVGDataSet();
    const dataView = new DataView(svgDataSet);
    dataView.parse(source, {
        type: "svg"
    });
    const parsedSvg = dataView.latestData, viewportRect = parseEnableBackgroundViewport(source);
    parsedSvg && viewportRect && !parsedSvg.viewBoxRect && !hasValidSize(parsedSvg) && (parsedSvg._vchartViewportRect = viewportRect), 
    svgSourceMap.set(key, dataView);
}

export function unregisterSVGSource(key) {
    svgSourceMap.has(key) ? svgSourceMap.delete(key) : warn(`map type of '${key}' does not exists.`);
}

export function getSVGSource(type) {
    return svgSourceMap.get(type);
}

export function clearSVGSource() {
    svgSourceMap.clear(), svgDataSet = null;
}
//# sourceMappingURL=svg-source.js.map
