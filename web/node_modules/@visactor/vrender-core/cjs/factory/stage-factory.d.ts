import type { IStage } from '../interface';
import type { IStageConstructor, IStageDependenciesFactory, IStageFactory } from './types';
export declare class StageFactory<TStage extends IStage = IStage> implements IStageFactory<TStage> {
    private readonly StageCtor;
    private readonly depsFactory?;
    constructor(StageCtor?: IStageConstructor<TStage>, depsFactory?: IStageDependenciesFactory);
    create(params?: {}): TStage;
}
