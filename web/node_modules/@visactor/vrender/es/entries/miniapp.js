var __rest = this && this.__rest || function(s, e) {
    var t = {};
    for (var p in s) Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0 && (t[p] = s[p]);
    if (null != s && "function" == typeof Object.getOwnPropertySymbols) {
        var i = 0;
        for (p = Object.getOwnPropertySymbols(s); i < p.length; i++) e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]) && (t[p[i]] = s[p[i]]);
    }
    return t;
};

import * as VRenderCore from "@visactor/vrender-core";

import { bootstrapVRenderMiniApp } from "./bootstrap";

import { installPendingRuntimeContributionModulesToApp } from "./runtime-contribution";

const {createMiniappApp: createMiniappApp} = VRenderCore;

function createMiniEnvVRenderApp(env, options = {}) {
    const {envParams: envParams} = options, entryOptions = __rest(options, [ "envParams" ]), app = bootstrapVRenderMiniApp(createMiniappApp(entryOptions), env, envParams);
    return installPendingRuntimeContributionModulesToApp(app), app;
}

export function createTaroVRenderApp(options = {}) {
    return createMiniEnvVRenderApp("taro", options);
}

export function createFeishuVRenderApp(options = {}) {
    return createMiniEnvVRenderApp("feishu", options);
}

export function createTTVRenderApp(options = {}) {
    return createMiniEnvVRenderApp("tt", options);
}

export function createWxVRenderApp(options = {}) {
    return createMiniEnvVRenderApp("wx", options);
}

export function createLynxVRenderApp(options = {}) {
    return createMiniEnvVRenderApp("lynx", options);
}

export function createHarmonyVRenderApp(options = {}) {
    return createMiniEnvVRenderApp("harmony", options);
}
//# sourceMappingURL=miniapp.js.map