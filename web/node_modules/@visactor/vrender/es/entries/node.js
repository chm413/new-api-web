var __rest = this && this.__rest || function(s, e) {
    var t = {};
    for (var p in s) Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0 && (t[p] = s[p]);
    if (null != s && "function" == typeof Object.getOwnPropertySymbols) {
        var i = 0;
        for (p = Object.getOwnPropertySymbols(s); i < p.length; i++) e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]) && (t[p[i]] = s[p[i]]);
    }
    return t;
};

import * as VRenderCore from "@visactor/vrender-core";

import { bootstrapVRenderNodeApp } from "./bootstrap";

import { installPendingRuntimeContributionModulesToApp } from "./runtime-contribution";

const {createNodeApp: createNodeApp} = VRenderCore;

export function createNodeVRenderApp(options = {}) {
    const {envParams: envParams} = options, entryOptions = __rest(options, [ "envParams" ]), app = bootstrapVRenderNodeApp(createNodeApp(entryOptions), envParams);
    return installPendingRuntimeContributionModulesToApp(app), app;
}
//# sourceMappingURL=node.js.map