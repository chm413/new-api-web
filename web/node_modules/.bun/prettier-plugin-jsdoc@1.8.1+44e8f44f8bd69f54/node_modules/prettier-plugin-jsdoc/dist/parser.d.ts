import { AllOptions } from "./types.js";
import { Parser } from "prettier";
type JsdocParser = (text: string, parsersOrOptions: Parameters<Parser["parse"]>[1], maybeOptions?: AllOptions) => ReturnType<Parser["parse"]>;
export declare const getParser: (originalParse: Parser["parse"], parserName: string) => JsdocParser;
export {};
